#include <stdio.h>
#include "libpower.h"
#include "resistancecalc.h"
//#include "tredjebiblioteket.h"

//program can be compiled with:
//     gcc main.c libpower.c resistancecalc.c -lm main
//the -lm flag is because of the math lib. Don't know why but it won't compile 
//otherwise. 


int main(int argc, char *argv[])
{

  //Hämta späningskälla (volt för de som inte pratar ström)
  //Hämta kopplingstyp {P | S}
  //Hämta antal komponenter = count
  
  //loopa på count
  
     //hämta resstansen (ohm för de som inte pratar ström)
     //stoppa in variabeln i en float array

  //end loop
  
  //Här bör all hämtning vara gjord. Därefter kan man köra de 
  //metoder som vi skrivit

  //Ersättningsresistans = calc_resstance()

  return 1; 
}
